<view>
  <view>
    <view>查找到的记录：</view>
    <view wx:for="{{queryResult}}" wx:key="item._id">{{ item._id }} <text bindtap="removeItem" data-_id="{{item._id}}">删除</text></view>
  </view>

  <view class="uploader">
    <view class="uploader-text">
      <text bindtap="getJokeList">查找数据</text>
    </view>
  </view>

    <view class="uploader">
    <view class="uploader-text">
      <text bindtap="insertJoke">插入数据</text>
    </view>
  </view>

</view>